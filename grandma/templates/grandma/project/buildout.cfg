[buildout]
parts = django

[django]
recipe = djangorecipe
project = {{ grandma_settings.project_name }}
version = 1.1
settings = production
unzip = true
eggs ={% for application in grandma_settings.applications.all %}{% if application.path %}
    {{ application.package }}{% endif %}{% endfor %}
